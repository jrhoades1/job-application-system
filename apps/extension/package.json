{
  "name": "job-app-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  }
}
